# listandstruct

Minimal helpers around pandas and pyarrow list/struct arrays.

## Pixi workflow

Install dependencies:

```bash
pixi install
```

Run tests:

```bash
pixi run test
```

Open a Python shell in the project environment:

```bash
pixi run python
```
